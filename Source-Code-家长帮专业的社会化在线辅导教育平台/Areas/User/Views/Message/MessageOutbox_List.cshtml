@using System.Data
@using ZoomLa.BLL.Helper
@using ZoomLa.BLL
@using ZoomLa.Common
@model ZoomLa.SQLDAL.SQL.PageSetting
@{
    B_User buser = new B_User();
    Model.cols = 5;
}
<div class="us_pynews u_cnt">
    <div id="EGV" >
       
        @foreach (DataRow dr in Model.dt.Rows)
        {
		
		<div class="message_list">
		  <span><a href="MessageRead?id=@dr["msgid"]">标题：@dr["Title"]</a> </span>
		  <div class="message_list_info"><small>收件人: @buser.GetUserNameByIDS(DataConverter.CStr(dr["Incept"]))</small></div>
		  <div class="message_list_time"><small>发送时间：@DateHelper.ToDate(dr["PostDate"])</small><small class="xuanze">勾选：<input type="checkbox" name="idchk" value="@dr["msgid"]" /></small>  </div>
		</div>
         
        }
       <div class="message_page">   @Html.Partial("_page2", Model) </div>
    </div>
</div>